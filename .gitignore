# The friend's repo — already its own git repository with ~935 MB of LFS
# data. Never vendor it in here; clone it separately if you need it.
reference/MLMAMidtermProject/

# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Editor / tool lock and cache files
~$*.md
~$*.docx

# Local tool and editor directories
.remember/
.claude/
.vscode/
.idea/
.pixi/
.DS_Store

# Audit intermediates - regenerate with tasks/audit_a*.py
Eric/cache/

# stray LaTeX output at repo root
texput.log
derived/pretrain_corpus.npz
derived/phase5_windows.npz
paper/_tmpl/*.log
